# Read data (integer)
n=int(input('Enter an integer: '))

# Find the next and previous numbers
o=n+1
m=n-1

# Output results
print(f"The next number for the number {n} is {o}")
print(f"The previous number for the number {n} is {m}")